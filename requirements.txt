# Direct dependencies only. Transitive packages are resolved by pip.
# For the exact tested environment, see requirements.lock.txt.

# --- Web framework ---
fastapi==0.139.0
uvicorn==0.51.0
python-multipart==0.0.32
slowapi==0.1.10

# --- Configuration ---
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2

# --- Database ---
SQLAlchemy==2.0.51
# Runtime, not dev-only: the container entrypoint runs `alembic upgrade head`.
alembic==1.19.2
# PostgreSQL driver. The [binary] extra ships prebuilt wheels, so the image
# needs no libpq or compiler. URL form: postgresql+psycopg://user:pw@host/db
psycopg[binary]==3.3.5

# --- Rate limiting ---
# Shared storage for slowapi (which already depends on `limits`). Without it
# rate limits are per worker process. See server/utils/rate_limiter.py.
redis==8.1.0

# --- Auth ---
python-jose==3.5.0
passlib==1.7.4
bcrypt==4.0.1
email-validator==2.3.0

# --- LLM ---
openai==2.46.0

# --- Observability ---
# Traces every LLM call (prompt, response, tokens, latency, cost) to Langfuse.
# Pulls in OpenTelemetry; a few MB beside torch's 520 MB.
langfuse==4.15.4

# --- RAG: embeddings, vector store, retrieval ---
chromadb==1.5.9
sentence-transformers==5.6.0
langchain-text-splitters==1.1.2
rank-bm25==0.2.2
numpy==2.5.1

# --- Document processing ---
pymupdf==1.28.0
pypdf==6.16.1

# --- Integrations: Gmail, Calendar, GitHub ---
google-api-python-client==2.198.0
google-auth-oauthlib==1.4.0
requests==2.34.2
python-dateutil==2.9.0.post0

# --- MCP ---
mcp==2.0.0
